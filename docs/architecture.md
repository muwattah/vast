# Architecture

Frontend (HTML/CSS/JS) → FastAPI → SQLAlchemy/SQLite

Modules: importers, valuation (comps, ARV, scenarios, outliers), AI providers (optional), investor tools (favorites, searches, notifications, audit).

Scrapers exist as disabled stubs only.
