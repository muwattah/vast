# Imports

1. POST /api/import/preview — validate without writing
2. POST /api/import/confirm — write valid rows
3. POST /api/import/comparables — JSON comps

CSV auto-maps Address/Price/Area/Postcode/EPC. Custom mapping via mapping_json form field.
